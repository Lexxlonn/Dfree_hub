import 'package:flutter/material.dart';

class Rehab extends StatelessWidget {
  const Rehab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Rehab Page'),
      ),
    );
  }
}
